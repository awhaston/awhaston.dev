const PROJECTS = document.getElementById('projects');
const PROJECTS_OVERFLOW = document.getElementById('projects-overflow');

PROJECTS.addEventListener('click', (e) => {
	PROJECTS_OVERFLOW.style.display = 'block';
});
